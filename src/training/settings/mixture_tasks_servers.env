{
  "name": "Basic",

  "min total time steps": 100, "max total time steps": 150,

  "min total servers": 4, "max total servers": 6,
  "server settings": [
    {
      "name": "Basic",
      "min storage capacity": 200, "max storage capacity": 300,
      "min computational capacity": 15, "max computational capacity": 30,
      "min bandwidth capacity": 15, "max bandwidth capacity": 25
    },
    {
      "name": "Large",
      "min storage capacity": 300, "max storage capacity": 450,
      "min computational capacity": 30, "max computational capacity": 40,
      "min bandwidth capacity": 30, "max bandwidth capacity": 40
    }
  ],

  "min total tasks": 200, "max total tasks": 250,
  "task settings": [
    {
      "name": "Basic",
      "min deadline": 6, "max deadline": 12,
      "min required storage": 50, "max required storage": 80,
      "min required computation": 25, "max required computation": 60,
      "min required results data": 10, "max required results data": 30
    },
    {
      "name": "Small",
      "min deadline": 6, "max deadline": 9,
      "min required storage": 25, "max required storage": 50,
      "min required computation": 15, "max required computation": 40,
      "min required results data": 8, "max required results data": 15
    },
    {
      "name": "Large",
      "min deadline": 12, "max deadline": 20,
      "min required storage": 90, "max required storage": 150,
      "min required computation": 40, "max required computation": 80,
      "min required results data": 20, "max required results data": 35
    }
  ]
}